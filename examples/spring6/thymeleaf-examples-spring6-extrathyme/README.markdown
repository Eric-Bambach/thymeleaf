This application can be run with:

```
$ mvn clean verify cargo:run
```

This will start an Apache Tomcat container serving the web application in `http://localhost:8080`

This iteration of the example is modified to continue to function in a manner consistent with the original, even if the number of teams in the mock repository changes.
